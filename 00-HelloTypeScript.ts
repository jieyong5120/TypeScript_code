console.log("HelloTypeScript...")
//这句代码用来打印一段文字内容

/*
* 第一步: ts -> js  使用tsc编译文件
* 第二步：运行js node运行js文件  node只能运行js文件
* */

/*
* 安装ts-node包管理器
*   简化ts -> js 转化 包管理器帮我们做了
*   只需要 执行命令ts-node + ts文件  就可以运行ts文件
* */

/*
* 现在用的IDE是webstorm
*   1：安装ts: npm install -g typescript
*   2：安装直接运行所需依赖包： npm install -g ts-node
*   3：在设置中安装Run Configuration for TypeScript插件后重启
*   4：重启后就会发现在ts文件中就会出现 run 了
* */